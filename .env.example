# Teams-to-Jira Processor Environment Variables
# Copy this file to .env and fill in your values

# ============================================================================
# REQUIRED CONFIGURATION
# ============================================================================

# OpenAI API Key (required for LLM transformations)
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# n8n Webhook URL (required for Jira integration)
# Format: https://your-n8n-instance.cloud/webhook/your-webhook-path
N8N_WEBHOOK_URL=https://your-n8n.cloud/webhook/teams-guncelleme

# ============================================================================
# OPTIONAL CONFIGURATION
# ============================================================================

# n8n API Key (optional, for webhook authentication)
N8N_API_KEY=

# Processor API Key (optional, require clients to authenticate)
# Generate with: openssl rand -hex 32
PROCESSOR_API_KEY=

# CORS Allowed Origins (comma-separated)
# Default: http://localhost:3000,http://localhost:8080
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080,https://teams.microsoft.com

# OpenAI Model (default: gpt-4.1-mini)
# Options: gpt-4.1-mini, gpt-4-turbo, gpt-4, gpt-3.5-turbo
OPENAI_MODEL=gpt-4.1-mini

# Server Host (default: 0.0.0.0)
HOST=0.0.0.0

# Server Port (default: 8090)
PORT=8090

# Data Directory (default: data)
# Where SQLite database and logs are stored
PROCESSOR_DATA_DIR=data

# ============================================================================
# ADVANCED CONFIGURATION
# ============================================================================

# Log Level (default: INFO)
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Database Connection Pool Size
DB_POOL_SIZE=5

# HTTP Request Timeout (seconds)
HTTP_TIMEOUT=30

# Enable Debug Mode (default: false)
# WARNING: Only use in development
DEBUG=false

# ============================================================================
# MCP SERVER (Optional)
# ============================================================================

# MCP Server Port (if running separately)
MCP_PORT=8080

# ============================================================================
# PRODUCTION RECOMMENDATIONS
# ============================================================================

# For production deployments:
# 1. Always set PROCESSOR_API_KEY
# 2. Restrict ALLOWED_ORIGINS to specific domains
# 3. Set LOG_LEVEL=WARNING or ERROR
# 4. Use HTTPS with reverse proxy (nginx/caddy)
# 5. Set HOST=127.0.0.1 if behind reverse proxy
# 6. Enable firewall rules
# 7. Regular database backups
# 8. Monitor disk space for PROCESSOR_DATA_DIR

# ============================================================================
# EXAMPLE PRODUCTION CONFIGURATION
# ============================================================================

# OPENAI_API_KEY=sk-prod-key-here
# N8N_WEBHOOK_URL=https://n8n.example.com/webhook/teams
# N8N_API_KEY=your-n8n-api-key
# PROCESSOR_API_KEY=very-long-random-secure-key
# ALLOWED_ORIGINS=https://teams.microsoft.com
# OPENAI_MODEL=gpt-4-turbo
# HOST=127.0.0.1
# PORT=8090
# LOG_LEVEL=WARNING
